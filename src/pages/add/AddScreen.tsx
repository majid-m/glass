import { FC } from 'react';
import { SafeAreaView } from 'react-native';

const AddScreen: FC = () => {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    );
};

export default AddScreen;