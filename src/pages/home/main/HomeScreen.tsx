import { FC } from 'react';
import { SafeAreaView } from 'react-native';


const HomeScreen: FC = () => {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    );
};

export default HomeScreen;